{
  "name": "Registration",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Nome completo"
    },
    "email": {
      "type": "string",
      "format": "email",
      "description": "Email para contato"
    },
    "phone": {
      "type": "string",
      "description": "Telefone para contato"
    },
    "institution": {
      "type": "string",
      "description": "Instituição que representa"
    },
    "type": {
      "type": "string",
      "enum": [
        "delegado",
        "observador",
        "convidado"
      ],
      "description": "Tipo de inscrição"
    },
    "representation": {
      "type": "string",
      "enum": [
        "usuario",
        "gestor",
        "trabalhador"
      ],
      "description": "Representação do inscrito (apenas para delegados)"
    },
    "access_code": {
      "type": "string",
      "description": "Código de acesso para certificado"
    },
    "certificate_authorized": {
      "type": "boolean",
      "default": false,
      "description": "Indica se o certificado está autorizado"
    },
    "status": {
      "type": "string",
      "enum": [
        "pending",
        "confirmed"
      ],
      "default": "pending",
      "description": "Status da inscrição"
    }
  },
  "required": [
    "name",
    "email",
    "phone",
    "institution",
    "type"
  ]
}